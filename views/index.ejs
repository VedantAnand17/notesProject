<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <h1 class="p-10 -mb-10 flex justify-center items-center text-white text-5xl">NOTES APP</h1>
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block w-full px-5 py-2 bg-zinc-800 rounded-lg outline-none" type="text" placeholder="Title goes here" name="title">
                <textarea class="block w-full px-5 py-2 mt-2 bg-zinc-800 resize-none rounded-lg outline-none" placeholder="Write your task details here" name="details"></textarea>
                <input class="mt-2 px-5 py-2 bg-blue-600 rounded-lg" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 justify-center flex-wrap">
            <% if(files.length > 0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800 text-white ">
                        <h1 class="text-3xl tracking-tighter flex-wrap overflow-hidden"><%= val.split(".txt").join('') %></h1>
                        <div class="flex w-full justify-between items-center mt-3">
                            <a class="text-blue-500 inline-block" href="/file/<%= val%>">Read More.</a>
                            <a class="bg-zinc-800" href="/edit/<%= val %>">Edit Title</a>
                        </div>
                    </div>
                    <% }) %>
            <% } else  { %>
                        <h3 class="text-white">No Tasks Yet</h3>
                    <% }%>
        </div>
    </div>
</body>
</html>